# Redirects for Cloudflare Pages
# Redirect default Cloudflare Pages subdomain to primary domain
https://unraid-api.pages.dev/* https://unraid-api.domalab.net/:splat 301!

# Redirect old URLs to new structure if needed
# /old-page /new-page 301
# /legacy/* /content/:splat 301

# Handle 404s
/* /404.html 404
